# Core TPM library
# Note: tpm2-pytss is now bundled with the package (no need to install separately)
# The bundled version includes all required TSS2 libraries (DLLs/.so files)
# Option 1: pytss (legacy - TPM 1.2 only, not recommended)
# pytss>=0.1.0
# Option 2: tpm2-pytss (bundled - TPM 2.0, included in package)
# tpm2-pytss is bundled - see BUILD.md for building instructions

# Cryptography
cryptography>=41.0.0

# Windows-specific (only needed on Windows)
pywin32>=305; sys_platform == 'win32'

# ASN.1 support (if needed)
pyasn1>=0.5.0
pyasn1-modules>=0.3.0

# HTTP client for server communication (for tpm_client.py)
requests>=2.31.0

